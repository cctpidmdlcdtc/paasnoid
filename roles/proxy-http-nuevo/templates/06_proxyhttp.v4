# proxy-http
# proxy-http querying lan
-A FORWARD -i br-{{ eth_hash }} -o {{ dev_BRIDGE_LAN }} -p tcp -m multiport --dports 80,443 -m conntrack --ctstate NEW -j ACCEPT
# Vagrant needs network traffic
-A FORWARD -i br-{{ eth_hash }} -o br-{{ eth_hash }} -j ACCEPT
